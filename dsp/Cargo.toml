[package]
name = "dsp"
version = "0.1.0"
edition = "2021"
keywords = ["dsp", "async", "audio", "embedded", "no-std"]
categories = ["embedded"]

[lib]
name = "dsp"
path = "src/lib.rs"

[dependencies]
libm = "0.2"
